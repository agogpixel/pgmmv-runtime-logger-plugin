import { createRuntimeLoggerPlugin } from './create-runtime-logger-plugin.function';

describe('createRuntimeLoggerPlugin', () => {
  it('is a function', () => expect(typeof createRuntimeLoggerPlugin).toBe('function'));
});
